{
  "name": "platform",
  "version": "1.0.0",
  "description": "BBQ platform",
  "main": "index.js",
  "scripts": {
    "bootstrap:dev": "npm run clean && node -r dotenv/config src/bootstrap.js",
    "clean": "rm -rf src/flavors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joe Kent",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1",
    "koa": "^2.5.1",
    "nodegit": "^0.22.0"
  },
  "devDependencies": {
    "dotenv": "^5.0.1"
  }
}
